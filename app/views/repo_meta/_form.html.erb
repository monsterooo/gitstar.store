<%= form_with(model: repo_metum, local: true) do |form| %>
  <% if repo_metum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(repo_metum.errors.count, "error") %> prohibited this repo_metum from being saved:</h2>

      <ul>
      <% repo_metum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :repo_id %>
    <%= form.number_field :repo_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
